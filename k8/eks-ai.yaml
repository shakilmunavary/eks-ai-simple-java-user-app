# ----------------------------------------
# Namespace
# ----------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: eks-ai
---
# ----------------------------------------
# Secret for GitHub Token
# ----------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: github-secret
  namespace: eks-ai
type: Opaque
data:
  GITHUB_TOKEN: Z2l0aHViX3BhdF8xMUFFNkNBVUkwT2Fid3l4VUc3eVp6X3lKZDhPY2Vzb1NDR3R2a1ozdzlRSDgyazFlb1Q5eXVJZFVJV0dqcmJUQ2tXRVQ1Q05RWGpDSVdYbWlx
---
# ----------------------------------------
# Deployment
# ----------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-ai-deployment
  namespace: eks-ai
  labels:
    app: eks-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eks-ai
  template:
    metadata:
      labels:
        app: eks-ai
    spec:
      containers:
        - name: eks-ai
          image: shakilahamed/eks-ai-simple-java-user-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-secret
                  key: GITHUB_TOKEN
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
      restartPolicy: Always
---
# ----------------------------------------
# Service
# ----------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: eks-ai-service
  namespace: eks-ai
spec:
  selector:
    app: eks-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
